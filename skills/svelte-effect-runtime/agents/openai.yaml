interface:
  display_name: "Svelte Effect Runtime"
  short_description: "Write strict SER-idiomatic Effect code"
  default_prompt: "Use $svelte-effect-runtime for all work in a SvelteKit app that has SER installed: SER owns Effect execution (never runSync/runPromise or your own runtime), handlers are Effect.gen, and components use direct yield* at SER sites."
