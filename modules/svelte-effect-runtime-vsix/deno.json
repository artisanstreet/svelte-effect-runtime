{
  "tasks": {
    "build": "deno task clean && deno run -A --config ../../deno.json ../../build/ext.ts",
    "clean": "deno run -A --config ../../deno.json ../../build/clean.ts svelte-effect-runtime-vsix",
    "package": "deno task build && deno run -A --config ../../deno.json ../../build/vsix.ts"
  }
}
