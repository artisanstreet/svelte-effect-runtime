{
  "tasks": {
    "build": "deno task clean && deno task prepare:runtime && deno run -A ../../build/lsp.ts",
    "clean": "deno run -A ../../build/clean.ts svelte-effect-runtime-language-server",
    "prepare:runtime": "deno task --cwd ../svelte-effect-runtime build && deno run -A ../../build/assets.ts svelte-effect-runtime-language-server",
    "start": "node ./.dist/server.cjs --stdio"
  }
}
