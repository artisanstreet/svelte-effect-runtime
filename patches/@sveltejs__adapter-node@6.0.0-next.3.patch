diff --git a/files/chunks/handler.js b/files/chunks/handler.js
index 390e1f8a980ff1d5f1fad04234194da6c68f3a4d..f7678d0f333c16f04b73b88d6f44db68f0154ba0 100644
--- a/files/chunks/handler.js
+++ b/files/chunks/handler.js
@@ -732,7 +732,7 @@ const ssr = async (req, res) => {
 	/** @type {Request} */
 	let request;
 	try {
-		request = getRequest({
+		request = await getRequest({
 			base: origin || get_origin(req.headers),
 			request: req,
 			bodySizeLimit: body_size_limit
diff --git a/files/dir.js b/files/dir.js
index b3d0e2d36594fc39dbe0a74cf6045989ab9a6fa7..d742583e8f0162c0817f4e1dbd3631f5c8c0fd8d 100644
--- a/files/dir.js
+++ b/files/dir.js
@@ -1,6 +1,6 @@
-import { dirname } from "node:path";
+import { dirname, resolve } from "node:path";
 import { fileURLToPath } from "node:url";
 //#region src/dir.js
-const dir = dirname(fileURLToPath(import.meta.url));
+const dir = resolve(dirname(fileURLToPath(import.meta.url)), "../..");
 //#endregion
 export { dir as t };
diff --git a/index.js b/index.js
index b0bea8b92a8fbc38570f09ce548198e1237916af..d422f1e056ce07a2aa7cbd0b0c46d736ff27f730 100644
--- a/index.js
+++ b/index.js
@@ -46,6 +46,7 @@ export default function (opts = {}) {
 			// pass means shared modules (e.g. `SvelteKitError` from `@sveltejs/kit`)
 			// aren't duplicated. See https://github.com/sveltejs/kit/issues/15755
 			const entries = `${tmp}/entries`;
+			const entries_pattern = entries.split(/[\\/]/).map(escape_regex).join('[\\\\/]');
 			builder.copy(files, entries);
 
 			const dir_id = join(entries, 'dir.js');
@@ -101,7 +102,7 @@ export default function (opts = {}) {
 						// accidentally replaced
 						name: 'adapter-node-replace-constants',
 						transform: {
-							filter: { id: new RegExp(escape_regex(entries)) },
+							filter: { id: new RegExp(entries_pattern) },
 							handler(_code, _id, { magicString }) {
 								if (!magicString) throw new Error('experimental.nativeMagicString is not enabled');
 								magicString
